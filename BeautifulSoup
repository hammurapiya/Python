from bs4 import BeautifulSoup as BS
import re

with open("HTML_Code.html", encoding="utf-8") as f:
    s = f.read()
soup = BS(s, "html.parser")
res = soup.find('span', {'class': 'total-users'}).string
result = re.findall("Нас уже ([0-9 ]+) человек", s)
print(result)

